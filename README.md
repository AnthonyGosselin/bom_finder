# Setup
1) Create virtual environment
2) Activate virtual environment
3) Install required packages:
```
pip install requirements.txt
```

# Execution:
1) Add all pdf files into a directory called `pdfs/` in this current (root) directory.

2) Run code:
```
python main.py
```
3) CSV files will be output into `csv/` directory
